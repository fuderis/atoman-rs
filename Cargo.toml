[package]
name = "atoman"
description = "A library for managing shared asynchronous data across threads. Provides safe concurrent access to application state with automatic synchronization and minimizes the need for manual lock management."
keywords = ["atomic", "static", "flag", "state", "config"]
categories = ["development-tools", "rust-patterns"]
version = "0.11.4"
license = "MIT"
authors = ["Bulat Sh. <synapdrake@ya.ru>"]
repository = "https://github.com/fuderis/atoman-rs"
readme = "README.md"
edition = "2024"

[workspace]
members = ["crates/macros", "crates/config", "crates/log"]
default-members = ["crates/macros", "crates/config", "crates/log"]
resolver = "3"

[workspace.dependencies]
proc-macro2 = "1.0.95"
quote = "1.0.39"
syn = { version = "2.0.100", features = ["full", "parsing", "extra-traits"] }
heck = "0.4"

[dependencies]
arc-swap = "1.8.0"
once_cell = "1.21.3"
macron = { version = "0.5.0", features = ["derive", "string", "async"] }
tokio = { version = "1.49.0", features = ["full"] }
ahash = "0.8.12"

atoman-macros = { version = "0.1.1", optional = true }
atoman-config = { version = "0.3.3", optional = true }
atoman-log = { version = "0.1.2", optional = true }

# atoman-macros = { path = "atoman-macros", optional = true }
# atoman-config = { path = "atoman-config", optional = true }
# atoman-log = { path  = "atoman-log", optional = true }

serde = { version = "1.0.228", features = ["derive"], optional = true }
serde_json = { version = "1.0.148", optional = true }
toml = { version = "1.0.0", optional = true }

log = { version = "0.4.29", optional = true }
tracing = { version = "0.1.44", optional = true }
tracing-subscriber = { version = "0.3.23", optional = true }

chrono = { version = "0.4.42", features = ["serde"], optional = true }
bytes = { version = "1.11.1", optional = true }
regex = { version = "1.12.3", optional = true }
fuzzy-cmp = { version = "0.1.3", optional = true }
colored = { version = "3.1.1", optional = true }

[features]
default = ["re-export"]
full = ["channel", "file", "config", "logger", "trace", "search"]
re-export = ["dep:atoman-macros"]
config = ["json-config", "toml-config", "dep:chrono"]
json-config = ["dep:atoman-config", "dep:serde", "dep:serde_json"]
toml-config = ["dep:atoman-config", "dep:serde", "dep:toml"]
logger = ["dep:atoman-log", "dep:tracing", "dep:tracing-subscriber",  "dep:chrono", "dep:bytes"]
trace = ["dep:regex", "dep:colored"]
channel = ["dep:serde", "dep:serde_json"]
file = ["dep:bytes", "dep:chrono", "dep:serde"]
search = ["dep:regex", "dep:fuzzy-cmp"]
tracing = ["dep:tracing"]
